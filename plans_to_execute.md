# خطة التنفيذ (Plans to Execute)

هذا الملف يحتوي على قائمة الأوامر/المهام المخطط تنفيذها في المشروع.
عند تنفيذ أي بند هنا، يُنقل إلى ملف [executed.md](executed.md) مع علامة ✅ وشرح مختصر لما تم، ويُحذف أو يُشطب من هذا الملف.

> **آلية العمل:**
> 1. أضف المهمة الجديدة هنا كبند `- [ ]` مع رقم تعريفي.
> 2. عند طلب التنفيذ من الذكاء الاصطناعي، ينفذ البند ثم يسجله في `executed.md` بنفس الرقم.
> 3. بعد التسجيل في `executed.md`، يوضع خط فوق البند هنا أو يُحذف.

---

## قائمة الانتظار

- [x] ~~#1 شارة لونية لعمود "التقدير" في شاشة أرشيف المشاريع (Archived/Index.vue)~~ ✅ تم التنفيذ — انظر executed.md
- [x] ~~#2 رابط تنزيل PDF مباشر من صف الجدول في شاشة أرشيف المشاريع (Archived/Index.vue)~~ ✅ تم التنفيذ — انظر executed.md
- [x] ~~#3 حذف مسار "اعتماد المشروع" (approve) المتضارب مع أرشفة ArchiveProjectModal~~ ✅ تم التنفيذ — انظر executed.md
- [x] ~~#4 استبدال بطاقة "في انتظار الموافقة" في لوحة التحكم ببطاقة "مشاريع تحت التنفيذ"~~ ✅ تم التنفيذ — انظر executed.md
- [x] ~~#5 إضافة الفصل الدراسي لعمود السنة في "آخر المشاريع المضافة"~~ ✅ تم التنفيذ — انظر executed.md
- [x] ~~#6 إصلاح عمود "الحالة" في "آخر المشاريع المضافة" + توحيد شارات الحالة~~ ✅ تم التنفيذ — انظر executed.md
- [x] ~~#7 فلتر نطاق سنوات (من – إلى) في التقرير السنوي~~ ✅ تم التنفيذ — انظر executed.md
- [x] ~~#8 ترويسة/تذييل طباعة كاملة (شعار + اسم الكلية + رقم صفحة)~~ ✅ تم التنفيذ — انظر executed.md
- [ ] #9 (غير مجدول بعد) إصلاح نص PDF العربي غير المفهوم في dompdf — مشكلة جوهرية في المكتبة (لا تدعم تشكيل الحروف العربية/bidi). يحتاج قراراً بين: (أ) معالجة النص عربياً قبل dompdf عبر مكتبة PHP للـ reshaping، أو (ب) التحول لمحرك PDF مختلف (Browsershot/Chromium، يتطلب Node.js على السيرفر). لم يُنفَّذ بعد — بانتظار طلب المستخدم.
- [x] ~~#10 توحيد "الممتحنين" و"المشرفين" في "أعضاء هيئة التدريس" + جدول الدرجات العلمية + حذف دوري المشرف والمشاهد~~ ✅ تم التنفيذ — انظر executed.md (لم يكن مجدولاً هنا مسبقاً، نُفّذ بناءً على طلب مباشر وخطة منفصلة)

- [x] ~~#11 منصة دورة حياة مشروع التخرج الكاملة — 5 أنظمة مترابطة عبر 8 مراحل تنفيذ~~ ✅ **الخطة كاملة منفَّذة** (Phase 1 → Phase 8، 2026-08-15 إلى 2026-08-17) — انظر executed.md من #13 إلى #20 لتفاصيل كل مرحلة.

  ## نظرة عامة
  توسيع النظام من "أرشفة مشاريع" إلى منصة متكاملة لإدارة دورة حياة مشروع التخرج: استيراد كشف الطلاب وإنشاء حساباتهم، بوابة طالب، بوابة مشرف، نظام إشعارات مركزي، ونظام نشر/طلب أفكار المشاريع. الفجوة الجوهرية المكتشفة أثناء الفحص: النظام الحالي 3 أدوار فقط (`super_admin`, `dept_manager`, `dept_staff`)، ولا يوجد أي مفهوم "حساب طالب" أو "حساب مشرف يسجّل دخول" — `FacultyMember` و`ProjectStudent` بيانات وصفية بدون حساب. لذا هذا العمل = توسيع نموذج الهوية (أدوار + حسابات) بالإضافة إلى 5 الأنظمة المطلوبة، وليس بناء 5 أنظمة فوق بنية جاهزة.

  ## القرارات المعمارية المعتمدة (بعد نقاش وموافقة صريحة)
  | القرار | التفاصيل |
  |---|---|
  | دورة الحياة (17 مرحلة) | جدول جديد موحّد `lifecycle_stages` (تعريف ثابت، seed) + `project_lifecycle_progress` (تتبّع تقدّم كل Project). **لا يُحذف ولا يُعدَّل** `project_status` (10 حالات) ولا `project_proposals.status` (5 حالات نصية) — يبقيان يعملان كما هما، والـ Timeline طبقة عرض/تتبّع إضافية فوقهما. |
  | حساب المشرف | إضافة `faculty_members.user_id` (nullable, FK→users, unique). المشرف يبقى بيانات (`FacultyMember`) + حساب دخول **اختياري** فقط عند الحاجة الفعلية — لا يتأثر أي عضو هيئة تدريس حالي بلا حساب. |
  | إنشاء حساب المشرف | زر **"إنشاء حساب دخول"** يُضاف في شاشة `FacultyMembers/Index.vue` الموجودة، بجانب كل عضو بلا حساب (لا استيراد جماعي). |
  | تفعيل حساب المشرف | اسم الدخول = `email` الموجود أصلاً في سجل `FacultyMember`. كلمة مرور مؤقتة = `phone_number` الموجود فعلاً (بدون إضافة أي حقل جديد لجدول `faculty_members`) + **إجبار تغيير كلمة المرور عند أول دخول** (`force_password_change`). |
  | حساب الطالب | جدول **`students`** جديد ومستقل تمامًا (لا تعديل على `project_students` الذي يبقى كما هو، بيانات أرشيفية وصفية فقط): `full_name`, `national_id`, `registration_number`, `department_id`, `specialization_id`, `semester`, `academic_year`, `date_of_birth` (عمود جديد مطلوب للاستيراد)، `user_id` (nullable, FK→users, unique)، `account_status`. |
  | تفعيل حساب الطالب | اسم الدخول = `registration_number`. كلمة مرور مؤقتة = `date_of_birth` (بصيغة صالحة ككلمة مرور، مثل `DDMMYYYY`) — **اختيار المستخدم صراحة رغم التحذير الأمني** (رقم وطني/تاريخ ميلاد بيانات شبه-عامة) + **إجبار تغيير كلمة المرور عند أول دخول** كحد أدنى تعويضي. |
  | آلية دخول الطالب برقم القيد ✅ **مُنفَّذ** | اسم الدخول = `registration_number` (كما طلب المستخدم). آليًا: عند إنشاء حساب الطالب يُولَّد بريد داخلي تلقائي غير حقيقي بصيغة `{registration_number}@students.local` ويُخزَّن في `users.email` (لا يُرسل إليه شيء، مجرد معرّف تقني فريد). الطالب يكتب رقم القيد فقط في خانة الدخول. **تنفيذياً تبيّن أن `LoginRequest` المشترك يحتاج تعديلاً فعلياً** (إضافة صرفة فقط: أي مدخل بدون `@` يُعامَل كرقم قيد ويُحوَّل للبريد المرتبط قبل `Auth::attempt`؛ أي بريد حقيقي يمر دون تغيير) — لا مفر تقنياً من هذا لأن الطالب يكتب رقم القيد وليس البريد التركيبي. اختير هذا كأصغر تعديل ممكن على كود مشترك حسّاس، واختُبر صراحة بـ Pest (8 اختبارات في `StudentLoginTest.php`) لضمان عدم كسر دخول الموظف بالبريد. |
  | رابط بوابة الطالب | **`/student`** (بدون تسجيل دخول) — صفحة عامة مستقلة قابلة للإعلان الخارجي (مثل فيسبوك)، تُعيد استخدام تصميم/مكوّن `Browse.vue` نفسه (بدون تكرار كود)، + زر **"دخول طالب"** بارز يقود إلى `/login` الموحّد. صفحة `/browse` الأصلية **تبقى بدون أي تعديل إطلاقًا**. المنطقة الخاصة بعد الدخول تحت بادئة `/student/*` (`/student/dashboard`, `/student/proposal`, `/student/project-ideas`...) محمية بـ `auth` + `role:student`. |
  | رابط بوابة المشرف | لا رابط منفصل — نفس `/login` الموحّد، فرق فقط بالدور (`role: supervisor`) والتوجيه بعد الدخول (Sidebar مختلفة + `/dashboard` يتفرّع حسب الدور كما يحدث حاليًا للأدوار الثلاثة). |
  | كشف التشابه | **لا نظام جديد** — توسيع `SearchService::detectSimilarity()` الموجود فعلًا ليُنتج نسبة % ومقارنة كلمات مفتاحية، بدل إعادة بناء. |
  | Versioning المقترحات وحجز الأفكار | **لا جدول نسخ جديد للمقترح نفسه** — الآلية موجودة فعلًا (`ProjectProposal.replaces_proposal_id` + `replacedBy()` + `STATUS_SUPERSEDED`)، يُعاد استخدامها ويُضاف فوقها جدول حجز جديد `proposal_reservations` لربط الطالب بالفكرة وتتبّع حالة الحجز فقط. |
  | الإشعارات | **لا جدول جديد** — جدول `notifications` القياسي لـ Laravel موجود ومفعّل فعلًا (يُستخدم حاليًا في `ProjectProposalStatusChanged`)، يُعمَّم فقط عبر إضافة Notification classes لكل حدث + مركز إشعارات UI موحّد. |

  ## أدوار Spatie جديدة
  `student`, `supervisor` تُضافان إلى `RoleSeeder` (الأدوار الثلاثة الحالية `super_admin`/`dept_manager`/`dept_staff` تبقى بلا أي تغيير).

  ## الجداول الجديدة (لا تكرار مع الموجود)
  `students`, `imported_student_batches`, `imported_student_rows`, `lifecycle_stages`, `project_lifecycle_progress`, `project_ideas`, `project_idea_requests`, `proposal_reservations`.
  تعديلات على جداول موجودة: `faculty_members.user_id`، `users.force_password_change`، `system_settings.registration_number_length` (لشرط طول رقم القيد المطلوب أثناء التحقق من الاستيراد).

  ## الصلاحيات
  Policies جديدة: `StudentPolicy`, `ProjectIdeaPolicy`, `ProjectIdeaRequestPolicy`. `RoleMiddleware` الحالي يدعم أي دور جديد تلقائيًا بلا تعديل. `ProjectProposalPolicy` يحتاج تعديلًا طفيفًا لدعم `student` (يرى مقترحه فقط) و`supervisor` (يرى مقترحات مشاريعه فقط) دون كسر صلاحيات `super_admin`/`dept_manager`/`dept_staff` الحالية.

  ## خطة التنفيذ بالمراحل (بالترتيب، كل مرحلة تُختبر backend+frontend+صلاحيات قبل الانتقال للتالية)
  - [x] ~~**Phase 1 — استيراد الطلاب وإنشاء الحسابات**~~ ✅ تم التنفيذ 2026-08-15 — انظر executed.md#13. (أدوار جديدة، migration جدول `students` + `imported_student_batches` + `imported_student_rows`، حسم نقطة تسجيل الدخول برقم القيد، شاشة استيراد Excel بنمط Preview→Confirm، التحقق من طول رقم القيد/12 رقم للرقم الوطني/عدم التكرار/اكتشاف الموجودين مسبقًا، إنشاء الحسابات بعد الاعتماد + كلمة مرور مؤقتة + إجبار تغيير + إشعار، شاشة "الطلاب المستوردون".) 264/265 اختبار ناجح (32 اختباراً جديداً). مؤجَّل عمداً لـ Phase 2: تقييد `/projects`/`/search` عن دور `student`.
  - [x] ~~**Phase 2 — بوابة الطالب**~~ ✅ تم التنفيذ 2026-08-16 — انظر executed.md#14. (`/student` العامة بإعادة استخدام `Browse.vue` نفسه + `/student/dashboard` + `/student/proposal/*` الخاصة، Timeline بصري محسوب تلقائياً من الحالات الموجودة — بدون جدول تتبع منفصل، تقديم/تعديل المقترح مع ربط فريق حقيقي بـ`student_id` ورؤية جماعية مشتركة، ملاحظات المشرف/القسم كحقلين جديدين يملأهما `dept_manager` حالياً، صفحة المشرف الحالي.) 278/279 اختبار ناجح (16 اختباراً جديداً). **قرار مسجَّل صراحة:** لم يُبنَ نظام إصدارات ملفات حقيقي (الملف القديم لا يزال يُستبدل عند الرفع) — بقرار المستخدم رغم مخالفته الطلب الأصلي. تقييد `/projects`/`/search` عن دور `student` لا يزال مؤجَّلاً (بقرار المستخدم من Phase 1، لم يُعالَج في Phase 2 أيضاً).
  - [x] ~~**Phase 3 — بوابة المشرف**~~ ✅ تم التنفيذ 2026-08-16 — انظر executed.md#15. (`faculty_members.user_id` + زر "إنشاء حساب دخول" في `FacultyMembers/Index.vue` (اسم الدخول = البريد الحقيقي الموجود، كلمة مرور مؤقتة = رقم الجوال)، دور `supervisor` جديد، Dashboard مشرف (كلي/قيد التنفيذ/منجز/يحتاج إجراء/توثيقات تحتاج مراجعة/جاهز للمناقشة/تكليفات كممتحن)، صفحة "مشاريعي" + Timeline لكل مقترح بإعادة استخدام `LifecycleTimelineService` من Phase 2 دون تعديل.) 293/294 اختبار ناجح (20 اختباراً جديداً). **قرار جديد محسوم أثناء هذه المرحلة (يُعدِّل قرار Phase 2):** المشرف أصبح يملأ `supervisor_note` بنفسه من بوابته (قدرة ضيقة، بدون صلاحية اعتماد/رفض)، مع بقاء قدرة `dept_manager` على ملئها أيضاً كما في Phase 2.
  - [x] ~~**Phase 4 — نظام الإشعارات**~~ ✅ تم التنفيذ 2026-08-16 — انظر executed.md#16. (نطاق مُحدَّد لـ4 أحداث حقيقية قابلة للتفعيل الآن: تقديم/إعادة تقديم مقترح → مدير القسم، تغيّر حالة المقترح → الفريق كاملاً + المشرف (وليس المنشئ فقط كما كان)، تكليف ممتحن → عضو هيئة التدريس، جاهزية للمناقشة → المشرف؛ غلاف موحَّد `title`/`message`/`url` عبر كل الإشعارات القديمة والجديدة؛ الجرس تحوَّل من رابط ثابت مقصور على `super_admin`/`dept_manager` نحو `/feedback` إلى قائمة منسدلة حقيقية + صفحة كاملة `/notifications` متاحة لكل الأدوار الخمسة؛ `/feedback` بقيت ميزة منفصلة تماماً (لوحة ملاحظات الزوار) لم تُمسّ.) 305/306 اختبار ناجح (18 اختباراً جديداً). بقية الأحداث الـ17 الأصلية مؤجَّلة لحين بناء أنظمتها الفعلية في المراحل اللاحقة (جدولة مناقشة، اعتماد توثيقات، إخلاء طرف، طلبات إشراف).
  - [x] ~~**Phase 5 — نظام أفكار المشاريع**~~ ✅ تم التنفيذ 2026-08-17 — انظر executed.md#17. (جدول `project_ideas` جديد (faculty_member_id/title/description/specialization_id/required_students_count/skills/keywords/notes/status)، `ProjectIdeaPolicy` (المشرف يدير أفكاره فقط، الطالب يرى المتاحة فقط)، CRUD كامل للمشرف تحت `/supervisor/ideas` بنموذج Modal مشترك بنمط `Departments/Index.vue`، تصفّح+تفاصيل للطالب تحت `/student/ideas` بفلتر تخصص وترقيم صفحات، روابط شريط جانبي لكلا الدورين.) 320/321 اختبار ناجح (15 اختباراً جديداً). **قرارات نطاق:** حذف مباشر بلا soft-delete (لا جدول يعتمد على `project_ideas` بعد)، لا صفحة رقابة لـ`dept_manager`/`super_admin` (لم تُطلب)، تغيير الحالة يدوي بالكامل من المشرف (الأتمتة تنتظر Phase 6).
  - [x] ~~**Phase 6 — طلبات الانضمام**~~ ✅ تم التنفيذ 2026-08-17 — انظر executed.md#18. (جدول `project_idea_requests` جديد (project_idea_id/student_id/message/status/decided_at)، `ProjectIdeaRequestPolicy`، الطالب يرسل طلبًا من صفحة تفاصيل الفكرة ويتابعه في صفحة "طلباتي"، المشرف يراجع طلبات كل فكرة في `/supervisor/ideas/{id}/requests` ويقبل/يرفض، عند القبول: الفكرة تصبح `reserved` أو `closed` تلقائيًا عند اكتمال `required_students_count` + إشعار الطالب، تطبيق "لا التزام مزدوج" عبر `hasActiveProposal()` الموجودة و`hasAcceptedIdeaRequest()` الجديدة.) 335/336 اختبار ناجح (15 اختباراً جديداً). **قرار حدود محسوم صراحة:** لا إنشاء `ProjectProposal`/`Project` فعلي عند القبول — ذلك تحديدًا مسؤولية Phase 7 (`proposal_reservations`) بنص الخطة الأصلية، تجنبًا لتكرار منطق سيُبنى هناك بدورة حياة مخصَّصة. أُصلح أثناء هذه المرحلة عطل منطقي في `ProjectIdeaPolicy::view()` كان يحجب الطالب عن رؤية فكرته الخاصة بعد قبولها.
  - [x] ~~**Phase 7 — Versioning وحجز الأفكار**~~ ✅ تم التنفيذ 2026-08-17 — انظر executed.md#19. (جدول `proposal_reservations` جديد (project_idea_id/student_id/project_idea_request_id/status)، قبول طلب انضمام Phase 6 الآن يُنشئ حجزًا تلقائيًا بحالة `reserved`، `ProposalReservationPolicy` تضبط الانتقالات (reserved→under_review→approved، أو →abandoned→released/finished) بترتيب صارم، صفحة "حجزي" للطالب وأزرار إدارة الحجز في صفحة مراجعة المشرف، سعة الفكرة تُحسب الآن من الحجوزات النشطة فعليًا بدل عدّاد ثابت.) 351/352 اختبار ناجح (16 اختباراً جديداً + تحديث اختبار Phase 6 واحد ليعكس القاعدة الجديدة). **قرار نطاق محسوم بالتزام حرفي بنص الخطة:** الحجز "يتتبّع حالته فقط" — لا تحويل تلقائي بعد لحجز معتمَد إلى `ProjectProposal`/`Project` فعلي (يبقى عملاً مفتوحًا خارج نطاق المراحل الثمانية). لا جدول نسخ جديد للمقترح نفسه — آلية `replaces_proposal_id` الموجودة لم تُمسّ.
  - [x] ~~**Phase 8 — كشف التشابه**~~ ✅ تم التنفيذ 2026-08-17 — انظر executed.md#20. (`SearchService::detectSimilarity()` أصبحت تُنتج نسبة تشابه % بمؤشر Jaccard على كلمات العنوان+الوصف بدل مطابقة نصية ثنائية، وتقارن ضد المشاريع **والمقترحات** معًا بدل المشاريع فقط، ورُبطت لأول مرة بمساري تقديم/تعديل المقترحات (الموظف والطالب) اللذين لم يكونا يستخدمانها إطلاقًا؛ روابط آمنة حسب الدور (لا رابط لمقترح لا يملك الطالب صلاحية رؤيته)؛ إصلاح عرض مكتشَف: تحذير المشروع لم يكن يظهر أبدًا لأن التحويل يذهب لصفحة العرض لا القائمة — أُضيف العرض لصفحات العرض الثلاث.) 359/360 اختبار ناجح (10 اختبارات جديدة + تحديث اختبارين قديمين لصيغة الإرجاع الجديدة). تحذير غير حاجب دائمًا كما هو مخطَّط — لا رفض تلقائي.
  - **🎉 الخطة الكاملة (#11) منفَّذة بأكملها الآن — Phase 1 حتى Phase 8، كل مرحلة موثَّقة ومُختبَرة في executed.md (#13 إلى #20).**

  ## ملاحظات تنفيذية عامة لكل الأنظمة أعلاه
  استخدام نفس Design System الحالي (Tailwind + مكوّنات `Modal`/`DataTable`/`ConfirmDelete`/شارات الحالة الموحّدة في `statusBadge.ts`) بدون أي تصميم مغاير. واجهات عربية RTL، Responsive، Loading/Empty/Error States، رسائل نجاح، Confirmation Dialogs. الالتزام الصارم بعدم حذف/كسر أي وظيفة أو جدول أو route موجود حاليًا — أي تعديل على كود مشترك (مثل منطق تسجيل الدخول في Phase 1) يُختبر بشكل صريح لضمان عدم تأثّر الأدوار الحالية الثلاثة.
