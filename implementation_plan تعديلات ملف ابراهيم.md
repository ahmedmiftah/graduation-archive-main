# خطة التعديلات الثلاثة على صفحة المشاريع

## نظرة عامة
ثلاثة تعديلات مترابطة على نظام إدارة المشاريع.

---

## التعديل 1 — إخفاء عمود القسم وإظهار رمز التخصص فقط

**المشكلة:** يوجد عمودان (القسم + التخصص) يعرضان الاسم الكامل ويأخذان مساحة زائدة.

**الحل:**
- حذف عمود "القسم" من جدول المشاريع في `Projects/Index.vue`
- إضافة حقل `code` للتخصص — الجدول الحالي `specializations` لا يحتوي على `code`
- **خيار مقترح:** استخدام أول حرفين/ثلاثة من الاسم كاختصار (بدون migration) لتجنب تغيير قاعدة البيانات
- أو إضافة migration جديد لإضافة `code` لجدول `specializations`

> [!IMPORTANT]
> هل تريد إضافة حقل `code` رسمي لجدول التخصصات (يحتاج migration + تحديث Seeder)؟ أم نكتفي بأول 3-4 أحرف من الاسم كاختصار مؤقت؟

---

## التعديل 2 — استبدال السنة الأكاديمية بالفصل الدراسي

**الحالة الحالية:**
- حقل `academic_year` (مثل: `2024-2025`) موجود في قاعدة البيانات
- حقل `semester` (مثال: `ربيع` أو `خريف`) موجود أيضاً بعد migration `2026_07_24`

**الحل:**
- في عمود الجدول: عرض `semester + academic_year` معاً (مثل: **ربيع 2024**) بدلاً من السنة وحدها
- في FilterPanel: إضافة فلتر الفصل (`semester`) بجانب أو بدلاً من `academic_year`

### الملفات المتأثرة:
#### [MODIFY] Projects/Index.vue
- تغيير رأس العمود من "السنة" إلى "الفصل الدراسي"
- عرض `semester + academic_year` مدمجَين
- إضافة فلتر semester في القائمة

#### [MODIFY] FilterPanel.vue
- إضافة dropdown للفصل الدراسي

#### [MODIFY] SearchService.php
- إضافة `semester` لخيارات الفلتر

---

## التعديل 3 — إضافة واجهة منفصلة للمشاريع المؤرشفة

### المنطق:
| الواجهة | الهدف | الحالة المستهدفة |
|---|---|---|
| `/projects` (الحالية) | مشاريع الفصل الحالي والسابق (جارية/منتهية/منقطعة) | كل الحالات ما عدا archived |
| `/projects/archived` (جديدة) | أرشيف المشاريع المكتملة مع إدخال مباشر | archived فقط |

### واجهة الأرشيف الجديدة `/projects/archived`:
- نموذج إدخال مشروع مؤرشف مباشرة يحتوي على:
  - بيانات المشروع الأساسية (عنوان، تخصص، مشرف، فصل، سنة)
  - بيانات الطلاب
  - تعيين الممتحنين مباشرة (حد أقصى 2)
  - ملاحظات التقييم لكل ممتحن
  - الدرجة النهائية
  - رفع PDF
- يُحفظ مباشرة بحالة `archived` (status_id = 1)

### الملفات المطلوبة:
#### [MODIFY] ProjectController.php
- إضافة `archivedIndex()` — يعرض المشاريع المؤرشفة فقط
- إضافة `archiveCreate()` — نموذج الإدخال المباشر
- إضافة `archiveStore()` — يحفظ مع الممتحنين والتقييمات مباشرة
- تعديل `index()` — يُخفي المشاريع المؤرشفة من العرض الرئيسي

#### [NEW] Projects/Archived/Index.vue — قائمة الأرشيف
#### [NEW] Projects/Archived/Create.vue — نموذج الإدخال المباشر

#### [MODIFY] routes/web.php
- إضافة `/projects/archived` و `/projects/archived/create`

#### [MODIFY] SearchService.php
- تعديل `searchProjects()` — استثناء `archived` من العرض الرئيسي افتراضياً

#### [MODIFY] AppSidebar.vue (إن وُجد)
- إضافة رابط "الأرشيف" في الشريط الجانبي

---

## Verification Plan
- تشغيل `php artisan test --filter=ProjectTest` للتحقق من عدم كسر اختبارات موجودة
- التحقق البصري من صفحة `/projects` (عدم وجود عمود القسم، ظهور الفصل)
- إنشاء مشروع مؤرشف من `/projects/archived/create` والتحقق من حفظ الدرجة والممتحنين مباشرة

## سؤال مفتوح
> [!IMPORTANT]
> هل تريد أن تُخفي المشاريع المؤرشفة نهائياً من صفحة `/projects` الرئيسية، أم تبقيها مرئية ولكن يمكن التصفية؟

