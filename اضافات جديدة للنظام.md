# اضافة وحدة الملاحظات والتقييم والإبلاغ عن المشكلات

## الهدف

تمكين الزوار من التفاعل مع المشاريع من خلال:

- إرسال ملاحظة.
- طرح سؤال.
- تقديم اقتراح.
- الإبلاغ عن مشكلة.
- تقييم المشروع.

## الوظائف الرئيسية

- عرض زر "إرسال ملاحظة" في بطاقات المشاريع في صفحة التصفح.
- عرض نافذة منبثقة (Modal) تحتوي على نموذج الملاحظات.
- حفظ الملاحظة في قاعدة البيانات مع بيانات المشروع والطلاب تلقائياً.
- إرسال إشعار تلقائي إلى الـ Super Admin ورئيس القسم المسؤول عن المشروع.
- إضافة لوحة إدارة ملاحظات محمية للأدوار المسموح بها.
- عرض إحصائيات الملاحظات في لوحة التحكم.
- إضافة سجل تدقيق لكل عملية على الملاحظة.
- حفظ تاريخ الردود السابقة.
- عرض متوسط التقييم وعدد التقييمات وتوزيعها وأحدث الملاحظات في صفحة المشروع.

## قاعدة البيانات

- ملف الهجرة `create_project_feedback_table`:
    - جدول `project_feedback`
    - الحقول: `id`, `project_id`, `visitor_name`, `visitor_email`, `feedback_type`, `title`, `message`, `rating`, `status`, `responded_at`, `project_title`, `department_name`, `student_names`, `created_at`, `updated_at`
- ملف الهجرة `create_project_feedback_replies_table`:
    - جدول `project_feedback_replies`
    - الحقول: `id`, `project_feedback_id`, `user_id`, `message`, `created_at`, `updated_at`
- ملف الهجرة `create_project_feedback_audit_logs_table`:
    - جدول `project_feedback_audit_logs`
    - الحقول: `id`, `project_feedback_id`, `user_id`, `action`, `notes`, `created_at`
- ملف الهجرة `create_notifications_table` (Laravel notifications):
    - جدول `notifications`

## النماذج (Models)

- `App\Models\ProjectFeedback`
    - علاقات: `project()`, `replies()`, `auditLogs()`.
    - ثوابت الأنواع والحالات.
- `App\Models\ProjectFeedbackReply`
    - علاقة `feedback()`.
    - علاقة `user()`.
- `App\Models\ProjectFeedbackAuditLog`
    - علاقة `feedback()`.
    - علاقة `user()`.

## Repository و Service

- `App\Repositories\ProjectFeedbackRepository`
    - عمليات الحفظ والاستعلام والفلاتر.
- `App\Services\ProjectFeedbackService`
    - منطق الأعمال، إنشاء الملاحظات، بيانات المشروع الإضافية، إشعارات، سجل النشاط.

## الطلبات (Form Requests)

- `App\Http\Requests\StoreProjectFeedbackRequest`
- `App\Http\Requests\ReplyProjectFeedbackRequest`
- `App\Http\Requests\UpdateProjectFeedbackStatusRequest`

## الصلاحيات (Policies)

- `App\Policies\ProjectFeedbackPolicy`
    - `viewAny`, `view`, `reply`, `updateStatus`, `archive`, `delete`
- تسجيل السياسة في `App\Providers\AuthServiceProvider`

## الإشعارات

- `App\Notifications\NewProjectFeedbackNotification`
    - قناة `database`
    - بيانات الإشعار: اسم المشروع، اسم الزائر، نوع الملاحظة، العنوان، التقييم، تاريخ الإرسال، مقتطف الرسالة.

## المتحكمات (Controllers)

- `App\Http\Controllers\ProjectFeedbackController`
    - حفظ الملاحظة من صفحة المشروع العامة.
- `App\Http\Controllers\Api\ProjectFeedbackApiController`
    - إدارة الفهرسة، الترشيح، التفاصيل، القراءة، الرد، تغيير الحالة، الحذف.

## التوجيهات (Routes)

- إضافة مسار `POST /projects/{project}/feedback` لحفظ الملاحظة.
- إضافة مسارات API محمية:
    - `GET /api/feedback`
    - `GET /api/feedback/{id}`
    - `PATCH /api/feedback/{id}/read`
    - `PATCH /api/feedback/{id}/status`
    - `POST /api/feedback/{id}/reply`
    - `DELETE /api/feedback/{id}`

## واجهة المستخدم (Vue)

- صفحة `resources/js/pages/Feedback/Index.vue`
    - جدول إدارة الملاحظات.
- صفحة `resources/js/pages/Feedback/Show.vue`
    - تفاصيل الملاحظة والردود.
- مكون `resources/js/components/Feedback/FeedbackModal.vue`
    - نموذج إرسال ملاحظة.
- مكون `resources/js/components/Feedback/StarRatingInput.vue`
    - تقييم النجوم من 1 إلى 5.
- مكون `resources/js/components/Feedback/FeedbackStatsCards.vue`
    - بطاقات إحصائية داخل لوحة التحكم.
- مكون `resources/js/components/Notification/NotificationBell.vue`
    - عدد الإشعارات غير المقروءة في الشريط.

## التعديلات على الواجهات الحالية

- `resources/js/pages/Public/Browse.vue`
    - زر إرسال ملاحظة في كل بطاقة مشروع.
- `resources/js/pages/Public/Show.vue`
    - عرض أحدث الملاحظات للتفاعل.
- `resources/js/pages/Dashboard.vue`
    - إحصائيات الملاحظات والتقييم.
- `resources/js/components/AppSidebar.vue`
    - رابط إلى لوحة الملاحظات للمشرفين ومديري الأقسام.

## الاختبارات

- `tests/Feature/Feedback/ProjectFeedbackSubmissionTest.php`
- `tests/Feature/Feedback/ProjectFeedbackAdminTest.php`
- `tests/Unit/Services/ProjectFeedbackServiceTest.php`
- `tests/Unit/Repositories/ProjectFeedbackRepositoryTest.php`

## ملاحظات عامة

- المشروع الحالي يستخدم Inertia/Vue، لذا سيتم حفظ واجهة إدارة الملاحظات داخل صفحات Inertia.
- أحمي الوصول إلى لوحة الملاحظات عبر `auth` و`role:super_admin,dept_manager`.
- الإشعارات المخزنة سيتم استخدامها لعرضها في جرس الإشعارات داخل لوحة التحكم.
- سأستخدم نمط MVC مع Repository و Service وفصل طلبات التحقق والسياسات.
